<!-- 小程序内不显示导航栏返回，H5 内部导航由页面自身承担 -->
<web-view wx:if="{{url}}" src="{{url}}" bindmessage="onMessage"></web-view>
<view wx:else style="color:#94a3b8;padding:40px 24px;font-size:14px">请先在 app.js 配置 WEB_URL，再把站点域名加入「业务域名」。</view>
